[package]
name = "decode-wasm"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
decode-core = { path = "../decode-core" }
wasm-bindgen = { workspace = true }
