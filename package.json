{
  "name": "decode-wasm",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 8000",
    "build:glue": "cargo build --release --target wasm32-unknown-unknown -p decode-wasm && wasm-bindgen target/wasm32-unknown-unknown/release/decode_wasm.wasm --out-dir js/pkg --target web && wasm-bindgen target/wasm32-unknown-unknown/release/decode_wasm.wasm --out-dir js/pkg-node --target nodejs",
    "test": "node js/adapters.test.mjs && node js/smoke.mjs",
    "bench": "node js/bench.mjs"
  }
}
